import 'package:flutter/material.dart';

/// Antique-atlas theme: aged parchment silhouette board, deep indigo ink,
/// brass accents -- distinct from the logic-puzzle catalog and from
/// Wordlink's warm paper theme.
class Palette {
  static const void_ = Color(0xFF11161F);
  static const panel = Color(0xFF1A2130);
  static const raised = Color(0xFF232C3F);

  static const parchment = Color(0xFFE8DFC8);
  static const parchmentDim = Color(0xFFCFC4A6);
  static const silhouette = Color(0xFF2B3A55);

  static const ink = Color(0xFFF0EBDA);
  static const brass = Color(0xFFC9A24B);
  static const haze = Color(0xFF8791A6);
  static const line = Color(0xFF2E3850);

  static const correct = Color(0xFF6FA37B);
  static const wrong = Color(0xFFD1685A);

  static const tierColors = {
    'easy': Color(0xFF6FA37B),
    'medium': Color(0xFFC9A24B),
    'hard': Color(0xFFD1685A),
  };
}
